README
=======================
To run the testing:
    $ julia -p 4
    >> @everywhere include("testing.jl")
    >> embed = test_word_embedding()

Used Julia Modules
======================
Iterators
Distances
IProfile
NumericExtensions
